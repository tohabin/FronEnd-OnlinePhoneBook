/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { IsEqualValidatordirectiveDirective } from './IsEqualValidatordirective.directive';

describe('Directive: IsEqualValidatordirective', () => {
  it('should create an instance', () => {
    const directive = new IsEqualValidatordirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
